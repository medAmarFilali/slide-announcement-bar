jQuery(window).load(function () {
  jQuery(".slick-slider").slick({
    autoplay: true,
    autoplaySpeed: slickOptions.autoplaySpeed,
    infinite: true,
    arrows: false,
    // dots: true,
  });
});
